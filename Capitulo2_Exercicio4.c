#include <stdio.h>
#include <stdlib.h>

/* Faca um programa que leia um numero inteiro e depois o imprima usando o operador "%f". Veja o que acontece. */

int main() {
    int x;

    scanf("%d", &x);
    printf("%f\n", x);
    return 0;
}